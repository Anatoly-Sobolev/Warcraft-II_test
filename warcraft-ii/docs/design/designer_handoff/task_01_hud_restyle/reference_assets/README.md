# Local reference assets

Положите сюда извлеченные PNG и screenshots/crops для дизайнера.

Эта папка исключена из Git и используется для локальных reference materials.
Runtime-заглушки для проекта кладутся в `content/assets/` как
`original_placeholder` с записью в manifest.

Ожидаемые подпапки:

```text
ui/
campaigns/
screenshots/
```
